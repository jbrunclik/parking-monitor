[project]
name = "parking-monitor"
version = "0.1.0"
description = "Parking spot monitor — API receiver and image processor"
requires-python = ">=3.12"
license = "MIT"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]
